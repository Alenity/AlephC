#version 330 core
out vec4 FragColour;

uniform vec3 objectColour;
uniform vec3 lightColour;

void main()
{
    float ambientStrength = 0.5;
    vec3 ambient = ambientStrength * lightColour;
    FragColour = vec4(ambient * objectColour, 1.0f);
}